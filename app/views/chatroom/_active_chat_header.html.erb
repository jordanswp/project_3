<div class="activeChatHeader hiddenUrl shadow8 d-flex justify-content-between align-items-center">
  <div class="d-flex flex-start"style="height: 4rem">
    <img class="img-fluid h-100 rounded mr-3" src="<%= image_url room.listing.image_url %>"/> 
      <div class="text-left mt-2">
        <div class="font-weight-bold"><%= @room.listing.title %></div>
        <p><%= @room.listing.price %></p>
      </div>
  </div>  
  <a href="<%= listing_path(@room.listing) %>"></a>
    <div class="personFont"><%= find_other_username(room, current_user) %></div>
</div>

