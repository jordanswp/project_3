<nav class="navBarOne navbar navbar-expand-md navbar-dark bg-navBg" style="max-height: 60px; min-width: 790px, box-shadow: 5px, 5px, 5px">
  <!-- logo -->
  <a class="navbar-brand" href="/"><%= image_tag("logos/logo4.png", width: '40px') %></a>
  <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- collapse nav container -->
  <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarCollapse">
    <div class="navbar-nav">
      <a href="/" class="nav-item nav-link d-none">Home</a>
    </div>
  <!-- search bar -->
    <form class="form-inline col-lg-7 d-flex justify-content-center" style="">
      <input type="text" class="form-control mr-sm-2 col-lg-7" placeholder="Search">
      <button type="submit" class="searchBtn btn btn-outline-light mr-1"><%= image_tag("logos/whitesearch.png", width: '20px') %></button>
      <a href="<%= new_listing_path %>" class="btn btn-outline-light ml-2">Create Listing</a>
    </form>
  <!-- right nav container -->
    <div class="userNav d-flex justify-content-end col-md-2">
      <% if user_signed_in? %>
      <p class="nav navbar-text navbar-right" style="font-weight: bold">Hello,</p>
      <ul class="nav navbar-nav">
        <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" style="font-weight: bold"><%= current_user.username %></a>
          <div class="dropdown-menu dropdown-menu-right">
            <a href="#" class="dropdown-item">Reports</a>
            <a href="#" class="dropdown-item">Settings</a>
          <div class="dropdown-divider"></div>
          <%= link_to 'Your Profile', current_user, class: 'dropdown-item'%>
          <%= link_to 'Log out', destroy_user_session_url, method: :delete, class: 'dropdown-item' %>
          </div>
        </li>
      </ul>
      <% else %>
      <a href="/users/sign_in" class="nav-item nav-link signNav">Sign In</a>
      <% end %>
    </div>
  </div>
</nav>